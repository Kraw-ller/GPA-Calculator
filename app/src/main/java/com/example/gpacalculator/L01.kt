package com.example.gpacalculator

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import androidx.cardview.widget.CardView
import com.example.task.R


class L01 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_l01)

//        val computeButton : Button = findViewById(R.id.compute_button)
//
//        var calculusAverage : Float
//        //todo
//        //create variables for each module average
//
//        computeButton.setOnClickListener {
//            val calculusAverageText : TextView = findViewById(R.id.calculus_average)
//            calculusAverage = moduleAvrg(findViewById<EditText?>(R.id.cc_cont).text.toString().toFloat(), findViewById<EditText?>(R.id.cc_exam).text.toString().toFloat())
//            calculusAverageText.setText(calculusAverage.toString())
//
//            //todo
//            //create a variable for the textView of the average of each module
//            //use the moduleAverage function
//            //set the text of the average of each module to its computed value
//            //use the function that computes the total average
//            //set the text of the total average textView to the total avrg value that is computed by the funtion
//        }
//    }
//
//    fun moduleAvrg(control : Float, exam: Float) : Float{
//        return ((control*0.4+exam*0.6).toFloat())
    }

//toDO
//create a function that computes the total average, it takes the modules as parameters and returns the total average

}